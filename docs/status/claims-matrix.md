# Babel Claims Matrix

<!--
status: ACTIVE
last_verified: 2026-07-21
-->
Date: 2026-06-29 (lite-gate at 269/269, P1 complete, daemon implemented, DAG workflow engine added, executorLoop reconciled, AO injection benchmark validated, calibration expanded to 40 tasks; see [BABEL_CONSOLIDATED_ROI_ROADMAP_2026-06.md](../plans/BABEL_CONSOLIDATED_ROI_ROADMAP_2026-06.md))


This matrix is the current truth ledger for public and internal-facing Babel positioning. Safe wording must track observed implementation and test evidence, not roadmap intent.

| Claim | Current verdict: GREEN/YELLOW/RED | Evidence | Unsafe wording | Safe wording | Proof needed |
| --- | --- | --- | --- | --- | --- |
| Babel has a claim-ready production gate for the validated DeepSeek-backed governed CLI lane. | GREEN | `babel benchmark production --json` is green at 8/8 with `claim_ready: true` for the scoped DeepSeek-backed governed CLI lane, including focused live-governance breadth, verifier/rollback proof, read-only Spark/Babel Full proof, plugin fixtures, and strict public-export proof. This does not demonstrate provider-agnostic production readiness, market parity, universal mandatory verifiers, production plugin ecosystem readiness, or mutating live subagents. | "provider-agnostic production AI control plane"; "operational control plane"; "safe autonomous subagent platform"; "Codex/Claude parity" | "The scoped DeepSeek-backed autonomous Coding Agent CLI lane has a claim-ready production gate, with provider-agnostic, market-parity, universal-verifier, production-plugin-ecosystem, and mutating-live-subagent claims excluded" | Keep `babel benchmark production --json` claim-ready, rerun required DeepSeek lanes when provider/model policy changes, and keep exclusions visible. |
| Babel has a structured Prompt OS. | YELLOW | `prompt_catalog.yaml` validates with 163 entries; `docs/architecture/ARCHITECTURE.md` documents layers; `babel-cli/src/schemas/agentContracts.ts` defines typed contracts. | "fully deterministic Prompt OS" | "structured optional Prompt OS mode with catalog validation and typed runtime contracts" | Golden compiled prompt snapshots across representative tasks and regression tests for prompt-stack stability. |
| Babel has a shipped lightweight CLI lane (Babel Lite). | GREEN | **2026-06-12 (Slice 4):** Coordinated multi-file small-fix (B5) implemented and verified. **2026-06-17 audit:** B5 multi-file small-fix complete — scope resolution (cap=4), implicit path detection, coordinated multi-file execution, and parity fixture all implemented. Live parity corpus (B7) measured live on Babel with **8/8 cells passing live** (2026-06-17). **2026-06-10 consolidation:** canonical surface is `babel "<task>"`, `babel plan`, `babel deep`, `babel undo`; removed verbs exit `1` with stderr hints. `AgentSession` in `babel-cli/src/agent/session.ts` dispatches daily routes. `test:lite-gate` is **269/269** (bounded repair, live tool stream, chat streaming, coordinated multi-file verifier loop, adversarial QA, arena evaluation, evidence merge — 2026-06-17 refresh). | "Babel Lite replaces full governance flow"; "Babel Lite is already a complete Codex/Claude replacement"; "daily worker works without API keys on arbitrary repos" | "Babel Lite is the daily CLI lane with `babel \"<task>\"`, `babel plan`, `babel deep`, and `babel undo`; offline trust demo and live daily fix are separate proof tiers; Codex parity and mutating subagents remain excluded" | CLI-green offline trust demo on fixture (`test:lite-trust-demo`); `test:live-small-fix:optional` on non-fixture repo when keys exist; `test:lite-worker-loop` green; live parity corpus has B5/B7 evidence. |
| Proposal-only Lite diffs do not apply changes. | GREEN | `AgentSession` routes `propose`/`diff`/`patch` through `proposalLane` with `autoApply: false`; `proposalLane.test.ts` asserts no source mutation and `runs/babel-lite/` artifacts. Daily `babel "<task>"` routes proposal language to read-only patch intent. | "proposal commands apply safe fixes by default" | "Proposal-only lanes do not apply source changes; artifacts land under `runs/babel-lite/`" | Keep mock/provider proposal tests green and extend live provider-backed proposal evidence when keys are available. |
| Prompt catalog validation is implemented. | GREEN | `powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\validate-catalog.ps1` passed with 163 entries, 0 warnings, 0 errors in the current run. | "catalog safeguards semantic correctness" | "catalog paths, IDs, and registered structure validate locally" | Semantic prompt-quality checks and mutation tests for catalog failure cases. |
| Resolver/compiler behavior is deterministic. | YELLOW | `tools/resolve-local-stack.ps1` now delegates to `babel resolve`; `npm --prefix .\babel-cli run test:local-stack-parity` passes across TS in-process, CLI, and PowerShell wrapper outputs; `tools/test-resolve-local-stack.ps1` passes 7 golden cases; `babel-cli/src/control-plane/stackResolver.ts` rejects unknown/inactive/wrong-layer/missing entries and errors or warns on conflicts depending on policy. | "deterministic execution" | "deterministic resolver/compiler behavior in tested scenarios" | Broaden golden output snapshots across representative task classes and keep conflict/dedup invariants in CI. |
| Model adapters are real runtime abstractions. | YELLOW | `03_Model_Adapters/` contains prompt-level adapters; runtime provider runners and model policy live separately under `babel-cli/src/`. **2026-06-29:** DeepSeek and DeepInfra API runners added with streaming improvements, expanding the provider surface. The underlying fragmentation (no single adapter contract tying prompt adapter, runner, model policy, tests, and fallback together) remains. | "clean model abstraction layer" | "prompt-level model adapters plus separate runtime provider runners" | One documented adapter contract tying prompt adapter, runner, model policy, tests, and fallback behavior together. |
| PLAN -> QA -> ACT governance exists. | GREEN/YELLOW | `00_System_Router/Babel_Runtime_Contracts-v1.0.md`, `babel-cli/src/schemas/agentContracts.ts`, and `babel-cli/src/pipeline.ts` implement the contract shape. **2026-06-05 (P7):** offline recorded-provider replay via `npm --prefix .\babel-cli run test:governance-replay` passes locally. Direct DeepSeek live proof via `npm --prefix .\babel-cli run test:live-governance` passed on 2026-06-05 with `deepseek-v4-flash`: backend run `runs/20260605_101732_regression-backend-verified-lane-validate-v9-uncomp` ended `QA_REJECTED_MAX_LOOPS` with no file changes; frontend run `runs/20260605_102207_regression-frontend-verified-lane-validate-v9-uncom` completed the no-execution verified lane with QA rejection evidence and no file changes. Sanitized snapshots live under `docs/status/live-governance-evidence/`. **2026-06-17 audit:** Adversarial QA module (`adversarialQALane.ts`) wired into `pipeline.ts` Stage 3 via `qaStage.ts`, uses a different model (step-flash via DeepInfra) from the SWE agent (deepseek), gated behind `BABEL_ADVERSARIAL_REVIEW=true` env var. **24 dedicated tests (16 unit + 8 integration) wired into CI via `test:unit` as of 2026-06-17.** Adversarial model is now configurable via `originalQaModel` parameter. | "QA gates execution"; "production governance loop" | "PLAN -> QA -> ACT governance has contract, replay, and bounded live DeepSeek evidence showing provider-backed QA rejection/no-execution behavior; broader production claims remain unproven" | Broaden live fixtures to include verifier-required completion, executor halt, dirty-worktree preservation, and repeated CI/secret-backed runs before stronger production or universal governance claims. |
| Verifiers are mandatory. | RED/YELLOW | `babel-cli/src/services/requiredVerifierContract.ts` handles declared required verifiers; audit found verifier-gated completion is scoped, not universal. | "mandatory verifiers" | "verifier-gated completion where declared or inferred verifier contracts apply" | Tests and docs enumerating every path that requires a verifier and every path where completion can proceed without one. |
| Terminal status normalization is implemented. | GREEN | `babel-cli/src/services/terminalStatus.ts` defines normalized statuses; unit tests cover exact-instruction drift, verifier failure, required-verifier states, and rollback precedence. | "terminal status safeguards correct task completion" | "terminal status normalization is implemented and locally tested" | End-to-end run artifacts showing status mapping under live CLI failures. |
| Rollback/worktree safety is implemented. | YELLOW | `babel-cli/src/services/worktreeSafety.ts` and tests cover snapshots, dirty-target refusal, protected paths, created-file cleanup, and rollback summaries. | "safe autonomous worker" | "rollback/worktree safety components are implemented and locally tested" | Hostile real-repo corpus with dirty files, generated artifacts, nested repos, and rollback-failure cases. |
| Subagent teams are safe for parallel read-only plan hardening. | GREEN/YELLOW | `babel full` now writes read-only Spark evidence and hardened-plan artifacts under `runs/babel-full`; `babel-cli/scripts/live_subagents_readonly_required.ts` records secret-gated DeepSeek read-only subagent proof. The older agent-team harness still covers read-only reviewer blocks, disjoint scoped merges, overlap refusal, and restore, but mutating live subagents remain disabled. | "Babel runs autonomous parallel coding agents"; "Spark workers can safely mutate live repos" | "Babel supports read-only Spark-style plan hardening before lead-owned governed execution; mutating live subagents remain excluded" | Mutating live subagent claims still require isolated worktree execution, conflict-resolution evidence, per-worker verifier artifacts, and hostile dirty-worktree recovery tests. |
| Plugin and hook extensibility is safe by default. | YELLOW | `babel plugins doctor --json` diagnoses manifests, trust levels, missing enabled plugins, duplicate surfaces, and enterprise-policy blocks. Third-party plugin corpus fixtures now cover read-only and local-mutating hook examples; enterprise policy blocks `external_network` trusts; hook runs emit before/after hash+byte-diff evidence. | "Babel has a production plugin ecosystem"; "plugins can safely extend all behavior" | "Babel has opt-in manifest plugins with explicit trust levels, policy gates, and local hook evidence for tested fixtures" | Third-party plugin corpus fixture evidence, hook diff evidence, and enterprise rollout tests are now captured; strict public scrub evidence remains a separate production gate. |
| `babel doctor` is production-grade. | YELLOW | `doctor --scope workspace --json` passed 14/14 on 2026-06-05. `doctor --scope all --json` is non-red when only documented external prerequisites are missing (for example `godot_td`), with repo-map warnings rather than hard blockers. | "operator-grade doctor across environments" | "`babel doctor` provides usable readiness signal, but release gates should accept documented external prerequisites as warnings, not blockers." | Keep `repo-map.json` aligned to actual workspace assumptions by documenting optional repos under `external_prerequisites` and validating that intentional blockers are still explicit. |
| Reliability matrix is operator-grade. | YELLOW | `npm --prefix .\babel-cli run reliability:matrix -- --help` and `--list` now work and list parser/schema aliases and 44 cases; script and argument parsing are implemented in `package.json`, `scripts/live_cli_reliability_matrix.ts`, and `src/services/liveCliReliabilityMatrixCliArgs.ts`; `src/services/liveCliReliabilityMatrix.test.ts` validates help/list output shape. | "complete operator UX and CI proof" | "reliability matrix command and help/list UX are implemented, with targeted unit coverage" | CLI smoke test (`scripts/test_live_cli_reliability_matrix_cli.ts`) in repo scripts/chain, resumable artifact docs, and full operator doc coverage. |
| DAG workflow engine replaces linear pipeline for parallelizable stages. | YELLOW | **2026-06-29:** `babel-cli/src/orchestrator/workflowEngine.ts` (526 lines) implements a DAG executor using Kahn's algorithm for topological sort with conditional edge resolution and parallel dispatch. `workflowNode.ts` (168 lines) defines the node abstraction. `workflowEngine.test.ts` (637 lines) provides test coverage. Wired into the streaming path. Replaces the strictly linear 4-stage sequence for workflows where stages can execute concurrently. | "DAG engine is production-hardened" | "DAG workflow engine is implemented with Kahn's algorithm topological sort and parallel dispatch; early-stage, scoped to streaming path" | Broaden DAG fixtures to cover all pipeline modes, adversarial edge cases, and rollback under partial DAG execution. |
| Babel records per-task model token/cost evidence. | GREEN/YELLOW | `babel-cli/src/services/costLedger.ts` writes canonical `cost_ledger.json` artifacts for governed, ask, small-fix, and Lite provider paths; runners attach pricing provenance, cache-token splits where available, and exact/conservative/unknown precision; `babel-cli/src/services/modelPricingRegistry.ts` centralizes pinned DeepSeek direct and DeepInfra rates; `npm --prefix .\babel-cli run audit:pricing` checks pinned rates against DeepInfra's models API and DeepSeek's official pricing page; focused tests cover ledger, pricing registry, runners, Lite artifacts, structured output, run stats, and cost tracker. | "billing-grade spend accounting"; "all model costs are exact"; "DeepInfra uses DeepSeek pricing" | "Babel writes a per-task cost ledger using pinned provider-specific rates, with exact/conservative/unknown precision and pricing-source provenance" | Keep `audit:pricing` in release checks, add saved live run artifacts with cache split examples, and document that provider invoices remain the billing source of truth. |

| Babel CLI supports incremental tool intent detection + human JIT veto with context anchoring and approved-roots symbol search (P0.1/P0.2). | **GREEN** | the relevant PR (merged [commit-hash]): IncrementalToolDetector (fences <|tool_start|>/end, JitDenialError, fingerprinting, split-payload tests); pathScanner (task tokens + approved roots anchor locking, TTY prompt); workspace_symbol_search (task-aware, read-only); InputCoordinator (TTY locks + JIT prompts); shouldJitApprove/promptUserJit (non-TTY denial, control-plane/anchor guards); session_state.json + deniedFingerprints persist. **2026-06-17 verification:** 5-scenario recorded-fixture replay test (`test:jit-veto-replay`, 6/6 tests passing) exercises the full flow — approve, multi-chunk, deny → JitDenialError → fingerprint persistence, PolicyBlockedDuplicateError on re-emission, cross-turn fingerprint tracking. Non-TTY guard verified. Context lock + symbol search exercised with live LLM. | "production-aimed autonomous JIT without human oversight" | "Babel CLI provides incremental tool intent streaming detection + human JIT veto (with session persistence and fingerprinting) for gated paths; verified via 5-scenario replay test (6/6 passing) + live LLM context tools exercise; P0.1/P0.2 complete." | Real TTY exercise with live model-driven tool fences remains optional (replay covers full code path). |
| Local Mode / CLI remediation backlog is tracked in-repo. | GREEN | docs/plans/LOCAL_MODE_CLI_REMEDIATION_BACKLOG_2026-05-21.md (2026-06-04): P0/P1 items, file map, test gaps (TG-01–TG-10). | "all gaps fixed" | "prioritized remediation backlog with acceptance criteria and test-gap IDs" | Close P0 items and update this matrix per P1-9. |
| Public-release validation is green. | GREEN | `npm --prefix .\babel-cli run build`, `npm run test:public-release`, and `npm run test:public-release:strict` are green in this workspace; strict mode promotes scrub warnings to failures while warning fixtures stay regression-only. | "public release validates production readiness" | "public-export gate is green for the scoped autonomous Coding Agent CLI with optional governed pipeline mode; production-agent and market-parity claims still require their own proof gates" | Maintainer release review remains required before publishing, and public-agent claims stay excluded. |
| Babel is comparable to Codex CLI, Claude Code, Gemini CLI, Aider, or OpenHands. | RED/YELLOW | `babel benchmark parity --json` creates the Phase 12 task matrix (8 tasks × 3 tools) with explicit `claim_ready: false` until all 24 cells are measured. **2026-06-06 (Wave 6):** Repeatable Babel automation exists for tasks 1–2 (`small_bug_fix`, `failing_test_repair`) via `npm --prefix .\babel-cli run parity:run-babel` and `babel-cli/src/fixtures/parity-corpus/` (offline_demo fixture scope only). `docs/guides/PARITY_BENCHMARK.md` documents `--fixture` merge for Codex/Claude cells. Product benchmark adds fixture-based Cursor-pattern scorecard dimensions (`plan_mode`, parallel review, checkpoint UX, verifier discipline). **No external Codex/Claude measurements recorded yet** — `claim_ready` remains false; do not promote parity GREEN. **2026-06-17 live parity:** Babel ran live on 5 parity tasks (2/5 passed, 40%). Single-hunk bug fixes (tasks 1-2) passed at 100% — competitive with Claude Code (93%) and Codex (~90%) on this category. Multi-file refactor and complex tasks (3-5) failed (1 verifier fail, 2 false completes). Directional comparison against published benchmarks (Terminal-Bench 2.0, SWE-bench, HUNK4J) shows Babel at 40% vs Codex 82.2% / Claude 70.1% / Gemini 74.8% — expected given DeepSeek-V4 vs GPT-5.5/Opus model tier gap. See `docs/status/babel-live-parity-comparison-2026-06-17.md`. **2026-06-29:** Calibration benchmark expanded from 23 to 40 tasks. Authority Order (AO) injection resistance benchmark built and validated on Llama — adversarial prompt injection resistance now measurable. | "competitor to Codex/Claude/Gemini"; "market parity"; "Babel beats Codex on parity benchmark" | "Babel is positioned as an autonomous coding-agent CLI, not yet at market parity; the parity harness and Babel cells for tasks 1–2 exist, but measured multi-tool parity does not" | Record Codex and Claude Code cells for ≥4/8 tasks with the same seeded repos; keep `claim_ready` false until 24/24 measured cells and evidenced comparisons exist. |
| Babel is ready for public demo. | YELLOW/RED | First-read docs expose limitations, public-export validation is green, and the scoped DeepSeek production gate is claim-ready. Market parity evidence, clean demo fixture, and public-agent positioning remain incomplete. **2026-06-17 audit:** Autonomous goal loop (`babel goal`) implemented in `goalLoop.ts` with plan->execute->verify->enrich->repeat cycle, gated behind `--experimental`. | "public-ready production agent"; "Codex/Claude competitor" | "autonomous Coding Agent CLI with optional governed pipeline mode — in private development with public-release trajectory; public-export gate is green but market parity, demo fixture, and production-agent claims remain incomplete" | Clean clone proof, demo fixture, passing tests without hidden skips, sample run artifacts, sanitized docs, and measured external-tool parity results before comparison claims. |
