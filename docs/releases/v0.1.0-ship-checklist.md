# Babel v0.1.0 Ship Checklist

- [x] README opening is benefit-first
- [x] `START_HERE.md` exists and is linked prominently
- [x] `examples/first-success.md` is readable quickly
- [x] `v9` is the public default story
- [x] `v8` is background-only in the public path
- [x] `pwsh -File tools\validate-catalog.ps1` passes
- [x] `npm run typecheck` passes
- [x] `npm run test:orchestrator-routing` passes
- [x] `npm run test:resolver` passes
- [x] No new scope is being added in this pass
- [x] Release note is ready
