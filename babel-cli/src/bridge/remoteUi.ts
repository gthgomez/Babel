/**
 * Stage-1 spike HTML was replaced by the installable PWA under `./remote-ui/`.
 * Kept as a named export so older imports fail loudly if they expect secrets.
 */
export const REMOTE_UI_HTML = '';
