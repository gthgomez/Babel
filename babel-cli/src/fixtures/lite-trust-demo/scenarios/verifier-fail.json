{
  "schema_version": 1,
  "fixture_type": "babel_lite_trust_demo",
  "scenario_id": "verifier_fail",
  "visibility": "public_example",
  "description": "Offline trust loop: mock fix applies a wrong answer, verifier fails; optional rollback-on-fail restores checkpoint.",
  "target_file": "src/math.js",
  "verifier_command": "npm test",
  "task": "Fix the failing Node test with a deliberate wrong answer for verifier-fail demo. Only edit src/math.js. Run npm test before completing.",
  "broken_implementation": "export const add = () => 0;\n",
  "mock_provider_answer": "export const add = (a, b) => a - b;\n",
  "fixed_implementation": "export const add = (a, b) => a + b;\n"
}
