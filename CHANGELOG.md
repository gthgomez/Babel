# Changelog

## Unreleased

- Public export generated from the private Babel development lane.
- Public release lane now uses branch-and-PR publishing instead of direct `main` updates.
- Public CI validates the exported CLI on Windows and Ubuntu and runs the required secret scan.
- Export validation now checks the exported catalog and exported TypeScript surface, not only the private source tree.
- Public docs now include current-state and vision material for community onboarding.
- See `docs/release/releases/` for release notes and checklist material.
