# Example Game Workspace Context

Use this project overlay for a generic game workspace. It is suitable for prototypes that need a stable root, reproducible asset paths, and visible evidence for gameplay-loop changes.

Public expectations:
- Keep generated files inside the selected game project root.
- Preserve engine project files and asset imports.
- Verify gameplay-loop changes with runnable evidence.
- Document platform-specific export assumptions.
