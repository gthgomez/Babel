# Example Mobile Reference Context

Use this project overlay for a local-first mobile reference application. It assumes a Kotlin/Compose client, a small persisted data model, and careful handling of user-visible state and offline recovery.

Public expectations:
- Keep examples generic and reproducible.
- Use placeholder package names and sample data only.
- Treat domain calculations as deterministic and testable.
- Prefer clear onboarding commands over private workspace assumptions.
