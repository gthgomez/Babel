# Validation Test Plan

- `babel skill validate skills/ts-zod` returns GREEN.
- `SKILL.md` covers strict typing, boundary parsing, Zod rules, and verification.
- Contracts parse as JSON.
- Default export remains blocked while status is experimental.
