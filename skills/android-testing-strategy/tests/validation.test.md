# Validation Test Plan

- `babel skill validate skills/android-testing-strategy` returns GREEN.
- `SKILL.md` includes test routing, ViewModel checks, device constraints, and enforcement output.
- Contracts parse as JSON.
