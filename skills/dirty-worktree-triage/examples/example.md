# Example

User task:

```text
What should I commit from this dirty repo?
```

Expected behavior:

- Run status and diff-stat commands.
- Sample diffs by cluster.
- Mark generated reports separately from source changes.
- Recommend explicit file sets, never `git add .`.
