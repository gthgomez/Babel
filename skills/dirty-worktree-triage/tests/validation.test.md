# Validation Test Plan

- `babel skill validate skills/dirty-worktree-triage` returns GREEN.
- `SKILL.md` includes snapshot, clustering, sampling, classification, and no-`git add .` rules.
- Contracts parse as JSON.
