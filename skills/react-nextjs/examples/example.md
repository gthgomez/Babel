# Example

User task:

```text
Refactor this dashboard page to load data correctly.
```

Expected behavior:

- Read the page, hooks, API client, and types.
- Preserve server/client boundaries.
- Add loading, error, and empty states.
- Avoid guessed API fields.
- Run typecheck and targeted UI tests/build.
